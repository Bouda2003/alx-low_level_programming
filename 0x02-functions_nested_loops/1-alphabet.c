#include "main.h"
/**
 * main - Entry point
 *
 * Describtion: Write a function that prints the alphabet, in lowercase.
 */

void print_alphabet(void);

char low;

for (low = 'a' ; low <= 'z' : low++)
{
	_putchar(low);
	_putchar('\n');
}
